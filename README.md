# odin-recipes-project

create a simple recipe site with only structured HTML with no CSS or js yet to practice what been learned so far from the Odin foundation course using:

1) paragraph tag
2) order/unorder list
3) img 
4) links
5) and other main tags like html body head

## practicing git and GitHub with command mainly like:

1) git add
2) git commit
3) git status
4) git log
5) git clone
6) git push
7) how to write commit message
8) create repository on GitHub and update it
9) publish it as pages on GitHub